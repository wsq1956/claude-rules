# CLAUDE.md

## 版本保留规则（最高优先级）

所有修改或输出的新内容必须另存为新版本，不得覆盖原有文件。

- 修改已有文件时：不直接覆盖原文件，而是另存为带版本后缀的新文件（如 `file_v2.ext`、`file_2026-09-19.ext`），原文件保持原样。
- 输出新内容时：如果同名文件已存在，同样另存为新版本，保留旧版本。
- 本规则适用于所有代码、文档、脚本、配置等输出物。
